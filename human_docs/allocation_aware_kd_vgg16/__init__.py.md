# `allocation_aware_kd_vgg16/__init__.py`

## 作用

- 为 `allocation_aware_kd_vgg16` 实验线提供简洁导出入口。
- 对外暴露 `run_allocation_aware_distillation_analysis`。

## 主要导出

- `run_allocation_aware_distillation_analysis`

## 使用方式

通常不直接单独导入这个文件，而是通过脚本：

```bash
python scripts/run_allocation_aware_kd_vgg16.py ...
```
